{
  "name": "@survarium-api/server",
  "dependencies": {
    "bluebird": "^3.3.3",
    "discord.js": "^11.4.2",
    "express": "^4.13.3",
    "mkdirp": "^0.5.1",
    "morgan": "^1.6.1",
    "sitemap": "^1.6.0",
    "survarium-api-client": "git+https://github.com/vaseker/survarium-api-client.git"
  },
  "scripts": {
    "deps": "npm i --no-optional && (cd v1 && npm i --no-optional) && (cd v2 && npm i --no-optional)"
  }
}
